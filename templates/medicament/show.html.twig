{% extends "baseback.html.twig" %}

{% block title %}Medicament{% endblock %}

{% block content %}
    <h1>Medicament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ medicament.libelle }}</td>
            </tr>
            <tr>
                <th>Fabricant</th>
                <td>{{ medicament.fabricant }}</td>
            </tr>
            <tr>
                <th>Duree_conservation</th>
                <td>{{ medicament.dureeConservation }}</td>
            </tr>
            <tr>
                <th>Forme</th>
                <td>{{ medicament.forme }}</td>
            </tr>
            <tr>
                <th>Gamme</th>
                <td>{{ medicament.gamme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_medicament_index') }}">back to list</a>

    <a href="{{ path('app_medicament_edit', {'id': medicament.id}) }}">edit</a>

    {{ include('medicament/_delete_form.html.twig') }}
{% endblock %}
