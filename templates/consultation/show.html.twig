{% extends "baseback.html.twig" %}

{% block title %}Consultation{% endblock %}

{% block content %}
    <h1>Consultation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultation.id }}</td>
            </tr>
            <tr>
                <th>Lien_visio</th>
                <td>{{ consultation.lienVisio }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ consultation.prix }}</td>
            </tr>
            <tr>
                <th>Lien_enregistrement</th>
                <td>{{ consultation.lienEnregistrement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consultation_index') }}">back to list</a>

    <a href="{{ path('app_consultation_edit', {'id': consultation.id}) }}">edit</a>

    {{ include('consultation/_delete_form.html.twig') }}
{% endblock %}
